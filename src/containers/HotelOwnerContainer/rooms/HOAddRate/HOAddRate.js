import React from 'react';
import ReactDOM from 'react-dom';

class HOAddRate extends React.Component {

    render() {
        return <span>HOAddRate</span>
    }
}

ReactDOM.render(<HOAddRate />, document.getElementById('root'));

export default HOAddRate;